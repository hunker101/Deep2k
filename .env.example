# Postgres
DATABASE_URL=postgres://deep2k:deep2k@localhost:5432/deep2k

# API
API_PORT=3000
ADMIN_TOKEN=change-me-to-a-long-random-string

# Dashboard
NEXT_PUBLIC_API_BASE=http://localhost:3000
DASHBOARD_PORT=3001

# Worker (dev only; in prod these come from wrangler.toml / KV)
WORKER_PORT=8787
WORKER_BACKEND_URL=http://localhost:3000/ingest
# JSON map of host -> { id, endpoint_path, script_path, secret } for dev lookup.
# Populated by `pnpm seed:pilot` and printed to stdout for copy-paste.
WORKER_SITES_JSON={}
